// lib/core/theme/colors.dart
//
// Colour palette anchored to the web app's brand. Primary = Atlantis blue.

import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF004AAD);
  static const primaryDark = Color(0xFF003680);
  static const accent = Color(0xFF1F8A4F);
  static const danger = Color(0xFFB3261E);
  static const warning = Color(0xFFC97800);
  static const surface = Color(0xFFFAFBFC);
  static const surfaceMuted = Color(0xFFF5F7FA);
  static const text = Color(0xFF0B1E33);
  static const textMuted = Color(0xFF555555);
}
